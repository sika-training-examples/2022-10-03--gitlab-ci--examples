image: alpine

before_script:
  - apk add curl

stages:
  - build
  - deploy

build:
  stages: build
  script: curl https://checkip.amazonaws.com

deploy:
  stages: deploy
  script: curl https://checkip.amazonaws.com
